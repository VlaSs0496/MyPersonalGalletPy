class Prueba():
    reason: str
    amount: float
    fecha: str

    def __init__(self, reason: str, amount: float, fecha: str):
        self.reason = reason
        self.amount = amount
        self.fecha = fecha
